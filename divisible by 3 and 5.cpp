#include<stdio.h>
int main()
{
    int num;
    printf("Enter the Number");
    scanf("%d",&num);
    if(num%3==0 && num%5==0);
    {
        printf("So the Number is divisible by 3 and 5");
    }
    
    
}